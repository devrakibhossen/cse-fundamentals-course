#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v; type 1
    // vector<int> v(4); type 2
    // vector<int> v(10, -2); type 3
    //<int> v2(v); type 4
    // int a[5] = {1,2,3,4,5};
    // vector<int> v(a,a+5);
    vector<int> v = {10, 20, 30, 40, 50};
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << endl;
    }
    // cout << v.size() << endl;
    return 0;
}